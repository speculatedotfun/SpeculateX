{
  "name": "speculate-subgraph",
  "version": "1.0.0",
  "description": "Subgraph indexing SpeculateCore events on BSC Testnet",
  "scripts": {
    "codegen": "graph codegen subgraph.yaml",
    "build": "graph build subgraph.yaml",
    "deploy": "goldsky subgraph deploy speculate-core-v2/production --version v1.0.0",
    "codegen:mainnet": "graph codegen subgraph.mainnet.yaml",
    "build:mainnet": "graph build subgraph.mainnet.yaml",
    "deploy:mainnet": "goldsky subgraph deploy speculate-core-mainnet/production --version v1.0.0"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.32.0"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.61.0"
  }
}

